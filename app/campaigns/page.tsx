import NavigationPlaceholder from "../components/navigation-placeholder";

export default function CampaignsPage() {
  return (
    <NavigationPlaceholder
      title="Campaign"
      description="Use the Campaign dashboard selectors to open an active Campaign Inspect report or compare an ended Promotion campaign against its specified benchmark."
    />
  );
}
